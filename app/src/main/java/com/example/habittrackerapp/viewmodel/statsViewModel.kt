
package com.example.habittrackerapp.viewmodel
/*
import android.content.Context
import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import com.example.habittrackerapp.models.Habit
import com.example.habittrackerapp.repository.HabitViewModel
import com.example.habittrackerapp.utils.Utils
import com.github.mikephil.charting.data.Entry

class StatsViewModel(val habitRepository: HabitViewModel) : ViewModel() {

    fun getEntriesForChart(entries: List<Habit>): List<Entry> {
        val list = mutableListOf<Entry>()
        for (entry in entries) {

        }

        return list
    }
}

class StatsViewModelFactory(private val context: Context):ViewModelProvider.Factory{
    override fun <T : ViewModel> create(modelClass: Class<T>): T {
        if (modelClass.isAssignableFrom(StatsViewModel::class.java)){
          //get instance of the database
            return StatsViewModel(habitRepository = HabitViewModel())
        }

    }
}*/
